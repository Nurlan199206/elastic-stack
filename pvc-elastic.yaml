### CLAIM FOR STORAGE
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: elastic-pv-claim #### sozdanie claim dlya kibana-pv-volume
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 
